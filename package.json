{
  "name": "deepseek-tui-web",
  "version": "0.5.3",
  "private": true,
  "description": "Local WebUI that brings DeepSeek TUI sessions, output, tasks, and settings into the browser.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "ws": "^8.20.0"
  }
}
